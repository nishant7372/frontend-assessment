const PUBLIC_URL = process.env.PUBLIC_URL;

const images = {
  avatar: `${PUBLIC_URL}/img/avatar.png`,
  logo: `${PUBLIC_URL}/img/logo.png`,
  html: `${PUBLIC_URL}/img/html.png`,
};

export default images;
